{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "halo-parity/event.schema.json",
  "type": "object",
  "required": ["eventId", "scenarioId", "renderer", "clockMode", "acknowledgedAt", "fiducialFrameZero"],
  "properties": {
    "clockMode": {"enum": ["diagnostic-seek", "normal-attestation"]},
    "renderer": {"enum": ["in-app-browser", "open-island-app"]}
  }
}
